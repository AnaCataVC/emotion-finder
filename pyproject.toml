[project]
name = "emotion-finder"
version = "1.0.0"
description = "Interactive Emotion Detector using Russell's Circumplex Model of Affect"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-fasthtml>=0.12.0",
    "scikit-learn>=1.3.0",
    "joblib>=1.3.0",
    "nltk>=3.8.1",
]

[tool.vercel]
entrypoint = "main:app"
